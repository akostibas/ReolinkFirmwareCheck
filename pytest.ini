[tool:pytest]
# Pytest configuration
testpaths = .
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    integration: marks tests as integration tests that hit real APIs (deselect with '-m "not integration"')